services:
  web:
    image: easycron-local:latest
    command: ['node', 'server.js']
    container_name: web
    networks:
      - local_network
    depends_on:
      - worker
      - scheduler
    env_file:
      - .env.compose
    ports:
      - '3000:3000'
    restart: unless-stopped
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://web:3000/api/health']
      interval: 30s
      timeout: 5s
      retries: 3

  worker:
    image: easycron-local:latest
    command: ['node', 'dist/worker/index.js']
    container_name: worker
    networks:
      - local_network
    depends_on:
      - redis
    env_file:
      - .env.compose
    restart: unless-stopped
    healthcheck:
      test: ['CMD', 'node', '-e', 'process.exit(0)']
      interval: 30s
      timeout: 3s
      retries: 3

  scheduler:
    image: easycron-local:latest
    command: ['node', 'dist/scheduler/index.js']
    container_name: scheduler
    networks:
      - local_network
    depends_on:
      - redis
    env_file:
      - .env.compose
    restart: unless-stopped
    healthcheck:
      test: ['CMD', 'node', '-e', 'process.exit(0)']
      interval: 30s
      timeout: 3s
      retries: 3

  redis:
    image: redis:7.2-alpine
    container_name: redis
    networks:
      - local_network
    env_file:
      - .env.compose
    restart: unless-stopped
    command: ['redis-server', '--requirepass', 'mysecretpassword']
    volumes:
      - redis_data_local:/data
    healthcheck:
      test: ['CMD', 'redis-cli', '-a', 'mysecretpassword', 'ping']
      interval: 30s
      timeout: 3s
      retries: 3

networks:
  local_network:
    driver: bridge

volumes:
  redis_data_local:
    driver: local
